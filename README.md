A stopwatch component rendered using React.js.

- Created using **create-react-app**
- Had to eject to use CSS modules.

## Running and Installing:

- Install the dependencies:

`yarn install`

- Then run the dev server using the command:

`yarn start`